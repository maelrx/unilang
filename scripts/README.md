# Scripts

Auxiliary project scripts are grouped here to keep the repository root focused on the package, tests, and primary docs.

- `benchmarks/`: ad hoc runtime and VPS benchmark runners
- `dev/`: quick inspection helpers for local API/type checks
- `smoke/`: lightweight manual verification scripts used during integration work
